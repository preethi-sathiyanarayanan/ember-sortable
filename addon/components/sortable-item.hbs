<div class="sortable {{if this.isDragingOver 'dragging-over'}} {{if @isDisabled 'disabled'}}"
		position={{@position}}
		sortable={{@sortElementSelector}}
		disabled={{@isDisabled}}
		id={{@content.id}}
		{{did-insert this.registerListener}}
    {{will-destroy this.unregisterListener}}
		...attributes>	
	{{yield (hash
		content=@content
	)}}
</div>